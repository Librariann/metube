extends layouts/main

block content
    .video__player
        video(src=`/${videoDetailInfo.fileUrl}`, controls=true)
    .video__info
        a(href=routes.editVideo(videoDetailInfo.id)) Edit video
        h5.video__title=videoDetailInfo.title
        span.video__views=videoDetailInfo.views
        p.video__description=videoDetailInfo.description